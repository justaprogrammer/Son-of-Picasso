<Window x:Class="SonOfPicasso.Windows.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:designTimeData="clr-namespace:SonOfPicasso.Windows.ViewModels.DesignTimeData"
        mc:Ignorable="d"
        d:DataContext="{x:Static designTimeData:DirectoryViewModelSampleData.SampleData}"
        Title="MainWindow" Height="350" Width="747.667">
    <Grid>
        <ListView  ItemsSource="{Binding Images}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <Image Source="{Binding Image}" />
                        <Label Content="{Binding File}" />
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Window>

