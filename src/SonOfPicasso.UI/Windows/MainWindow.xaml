<reactiveUi:ReactiveWindow x:TypeArguments="interfaces:IApplicationViewModel" x:Class="SonOfPicasso.UI.Windows.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:interfaces="clr-namespace:SonOfPicasso.UI.Interfaces"
        xmlns:reactiveUi="http://reactiveui.net"
        xmlns:views="clr-namespace:SonOfPicasso.UI.Views"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <DockPanel>
        <Menu DockPanel.Dock="Top" Width="Auto" Height="20">
            <MenuItem Header="_File">
                <MenuItem x:Name="NewAlbum" Header="New Album" Click="NewAlbum_Click"/>
                <Separator />
                <MenuItem x:Name="AddFolder" Header="_Add Folder" Click="AddFolder_Click"/>
                <MenuItem x:Name="AddFile" Header="Add Folder" Click="AddFile_Click"/>
            </MenuItem>
            <MenuItem Header="_Edit" />
            <MenuItem Header="_View" />
            <MenuItem Header="_Folder" />
            <MenuItem Header="_Picture" />
            <MenuItem Header="_Create" />
            <MenuItem Header="_Tools" />
            <MenuItem Header="_Help" />
        </Menu>

        <DockPanel>
            <ListView Name="FoldersListView" 
                      DockPanel.Dock="Left" 
                      MinWidth="300"
                      VirtualizingStackPanel.VirtualizationMode="Recycling"
                      VirtualizingStackPanel.ScrollUnit="Pixel">
                <ListView.ItemTemplate>
                    <DataTemplate DataType="{x:Type interfaces:IImageFolderViewModel}">
                        <reactiveUi:ViewModelViewHost ViewModel="{Binding}" />
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            <ListView Name="ImagesListView" 
                      VirtualizingStackPanel.VirtualizationMode="Recycling"
                      VirtualizingStackPanel.ScrollUnit="Pixel">
                <ListView.ItemTemplate>
                    <DataTemplate DataType="{x:Type interfaces:IImageViewModel}">
                        <reactiveUi:ViewModelViewHost ViewModel="{Binding}" />
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </DockPanel>
    </DockPanel>
</reactiveUi:ReactiveWindow>
